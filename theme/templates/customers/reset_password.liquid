{{ 'component-customer.css' | asset_url | stylesheet_tag }}

<div class="customer-section">
  <div class="customer-container">
    <div class="customer-header">
      <h1>{{ 'customer.reset_password.title' | t }}</h1>
      <p>{{ 'customer.reset_password.subtext' | t }}</p>
    </div>

    <div class="customer-box">
      {%- form 'reset_customer_password' -%}
        {%- if form.errors -%}
          <div class="customer-error-list" tabindex="-1" autofocus>
            {{ form.errors | default_errors }}
          </div>
        {%- endif -%}

        <div class="customer-form-group">
          <label for="password">{{ 'customer.reset_password.password' | t | default: 'New Password' }}</label>
          <input
            type="password"
            name="customer[password]"
            id="password"
            autocomplete="new-password"
            required
            {% if form.errors contains 'password' %}aria-invalid="true"{% endif %}
          >
        </div>

        <div class="customer-form-group">
          <label for="password_confirmation">{{ 'customer.reset_password.password_confirm' | t | default: 'Confirm Password' }}</label>
          <input
            type="password"
            name="customer[password_confirmation]"
            id="password_confirmation"
            autocomplete="new-password"
            required
            {% if form.errors contains 'password_confirmation' %}aria-invalid="true"{% endif %}
          >
        </div>

        <div class="customer-form-actions">
          <button type="submit" class="btn-primary" style="width: 100%; padding: 1rem;">
            {{ 'customer.reset_password.submit' | t }}
          </button>
        </div>
      {%- endform -%}
    </div>
  </div>
</div>
